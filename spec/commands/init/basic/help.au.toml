program = { env = "AUREUM_TEST_EXEC" }
program_arguments = ["init", "--help"]

expected_stdout = """
Initialize a new config file

Usage: aureum init [OPTIONS] [PATH]

Arguments:
  [PATH]  Where to save the config file (Recommended file extension: .au.toml)

Options:
      --print  Print to stdout instead of writing to a file
  -h, --help   Print help
"""
expected_stderr = ""
expected_exit_code = 0
