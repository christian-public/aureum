program = { from_env = "AUREUM_TEST_EXEC" }


# The initial TUI frame is emitted when using --record (no leading --- separator).
[[test]]
id = "initial-frame"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/failing-stdout-only.toml" },
]
stdin = "q\n"

expected_stdout = { from_file = "snapshots/record-initial-frame.txt" }
expected_exit_code = 1


# Arguments containing spaces are single-quoted so the command can be pasted into a terminal.
[[test]]
id = "spaced-arg-is-quoted"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/failing-stdout-spaced-arg.toml" },
]
stdin = "q\n"

expected_stdout = { from_file = "snapshots/record-spaced-arg.txt" }
expected_exit_code = 1


# Pressing a navigation key emits a --- separator followed by the new frame.
[[test]]
id = "key-press-emits-separator"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/failing-stdout-only.toml" },
]
stdin = "1\nq\n"

expected_stdout = { from_file = "snapshots/record-tab-switch-to-expected.txt" }
expected_exit_code = 1


# On an unconfigured field, pressing [1] Expected and [3] Diff in sequence are both no-ops.
[[test]]
id = "not-checked-field-view-switch-disabled"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/failing-stdout-only.toml" },
]
stdin = "e\n1\n3\nq\n"

expected_stdout = { from_file = "snapshots/record-stderr-not-checked-view-switch-disabled.txt" }
expected_exit_code = 1


# Pressing i switches to the stdin view, showing line numbers on the right (like Got view).
[[test]]
id = "stdin-view"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/failing-cat-with-stdin.toml" },
]
stdin = "i\nq\n"

expected_stdout = { from_file = "snapshots/record-stdin-view.txt" }
expected_exit_code = 1


# When two fields fail, Enter after deciding the first auto-navigates to the second undecided field.
[[test]]
id = "multi-field-navigation"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/failing-stdout-and-exit-code.toml" },
]
stdin = "s\nenter\ns\nenter\nq\n"

expected_stdout = { from_file = "snapshots/record-multi-field-navigation.txt" }
expected_exit_code = 1


# Pressing l from the diff view opens the list with the current test highlighted.
[[test]]
id = "list-initial-frame"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/two-failing-stdout-tests.toml" },
]
stdin = "l\nq\n"

expected_stdout = { from_file = "snapshots/record-list-initial-frame.txt" }
expected_exit_code = 1


# Pressing Down in the list moves selection to the next test.
[[test]]
id = "list-navigate"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/two-failing-stdout-tests.toml" },
]
stdin = "l\ndown\nq\n"

expected_stdout = { from_file = "snapshots/record-list-navigate.txt" }
expected_exit_code = 1


# Pressing Enter on a test in the list opens its diff view.
[[test]]
id = "list-jump-to-test"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/two-failing-stdout-tests.toml" },
]
stdin = "l\ndown\nenter\nq\n"

expected_stdout = { from_file = "snapshots/record-list-jump.txt" }
expected_exit_code = 1


# Decision indicators show [⊘] for skipped tests and [ ] for undecided.
[[test]]
id = "list-decision-indicator"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/two-failing-stdout-tests.toml" },
]
stdin = "s\nenter\nl\nq\n"

expected_stdout = { from_file = "snapshots/record-list-decision-indicator.txt" }
expected_exit_code = 1


# Partial decisions (only one of two failing fields decided) show [ · ] in the list.
[[test]]
id = "list-partial-decision"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/failing-stdout-and-exit-code.toml" },
]
stdin = "s\nl\nq\n"

expected_stdout = { from_file = "snapshots/record-list-partial-decision.txt" }
expected_exit_code = 1


# Scrolling past the last content line is clamped so at least one line stays visible.
[[test]]
id = "scroll-clamped-to-last-line"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/failing-stdout-only.toml" },
]
stdin = "down\ndown\ndown\ndown\ndown\nq\n"

expected_stdout = { from_file = "snapshots/record-scroll-clamped.txt" }
expected_exit_code = 1


# After pressing Down past the last line, the first Up immediately scrolls back up by one.
[[test]]
id = "scroll-up-after-excess-down"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/failing-stdout-only.toml" },
]
stdin = "down\ndown\ndown\nup\nq\n"

expected_stdout = { from_file = "snapshots/record-scroll-up-after-excess-down.txt" }
expected_exit_code = 1


# Pressing Esc in the list returns to the previously viewed test, ignoring any navigation done in the list.
[[test]]
id = "list-esc-goes-back"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/two-failing-stdout-tests.toml" },
]
stdin = "l\ndown\nesc\nq\n"

expected_stdout = { from_file = "snapshots/record-list-esc-goes-back.txt" }
expected_exit_code = 1


# Pressing p mid-way through a multi-field test preserves the decisions made so far.
[[test]]
id = "go-back-preserves-partial-decisions"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/failing-stdout-and-exit-code.toml" },
]
stdin = "s\nenter\np\nq\n"

expected_stdout = { from_file = "snapshots/record-go-back-preserves-partial-decisions.txt" }
expected_exit_code = 1


# Pressing an unbound key after staging a skip does not clear the staged decision.
[[test]]
id = "unbound-key-does-not-clear-staged-skip"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/failing-stdout-only.toml" },
]
stdin = "s\nd\nq\n"

expected_stdout = { from_file = "snapshots/record-skip-then-unbound.txt" }
expected_exit_code = 1


# Pressing p after advancing to the second test goes back to the first and restores its decision.
[[test]]
id = "go-back-restores-decision"
program_arguments = [
    "test",
    "--record=100x20",
    { path_of_file = "helpers/two-failing-stdout-tests.toml" },
]
stdin = "s\nenter\np\nq\n"

expected_stdout = { from_file = "snapshots/record-go-back-restores-decision.txt" }
expected_exit_code = 1
