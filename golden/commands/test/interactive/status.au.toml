program = { from_env = "AUREUM_TEST_EXEC" }


# Status area starts empty: no message shown when on a failing field that is undecided.
[[test]]
id = "empty-when-on-failing-undecided-field"
program_arguments = [
    "test",
    "--record=120x20",
    { path_of_file = "helpers/failing-stdout-only.toml" },
]
stdin = "q\n"

expected_stdout = { from_file = "snapshots/status-empty.txt" }
expected_exit_code = 1


# Switching to a non-failing output field (stderr is NotChecked) shows the navigation hint.
[[test]]
id = "non-failing-output-field"
program_arguments = [
    "test",
    "--record=120x20",
    { path_of_file = "helpers/failing-stdout-only.toml" },
]
stdin = "e\nq\n"

expected_stdout = { from_file = "snapshots/status-non-failing-field.txt" }
expected_exit_code = 1


# Switching to stdin shows the same navigation hint.
[[test]]
id = "stdin-field"
program_arguments = [
    "test",
    "--record=120x20",
    { path_of_file = "helpers/failing-stdout-only.toml" },
]
stdin = "i\nq\n"

expected_stdout = { from_file = "snapshots/status-stdin-field.txt" }
expected_exit_code = 1


# Pressing Enter on a failing field without a decision shows the error message.
[[test]]
id = "enter-on-undecided-failing-field"
program_arguments = [
    "test",
    "--record=120x20",
    { path_of_file = "helpers/failing-stdout-only.toml" },
]
stdin = "enter\nq\n"

expected_stdout = { from_file = "snapshots/status-enter-undecided.txt" }
expected_exit_code = 1


# With a pending decision that would resolve all fields, shows "go to the next test."
[[test]]
id = "pending-decision-last-field"
program_arguments = [
    "test",
    "--record=120x20",
    { path_of_file = "helpers/failing-stdout-only.toml" },
]
stdin = "a\nq\n"

expected_stdout = { from_file = "snapshots/status-all-decided.txt" }
expected_exit_code = 1


# With a pending decision but other fields still undecided, shows "go to the next field."
[[test]]
id = "pending-decision-more-fields-remain"
program_arguments = [
    "test",
    "--record=120x20",
    { path_of_file = "helpers/failing-stdout-and-exit-code.toml" },
]
stdin = "a\nq\n"

expected_stdout = { from_file = "snapshots/status-pending-next-field.txt" }
expected_exit_code = 1


# Pressing a then navigating to another field reverts the pending decision.
[[test]]
id = "pending-decision-reverts-on-navigate"
program_arguments = [
    "test",
    "--record=120x20",
    { path_of_file = "helpers/failing-stdout-only.toml" },
]
stdin = "a\ne\nq\n"

expected_stdout = { from_file = "snapshots/status-pending-reverts.txt" }
expected_exit_code = 1
