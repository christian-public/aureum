program = { from_env = "AUREUM_TEST_EXEC" }
program_arguments = ["run", "--help"]

expected_stdout = """
Run programs from test specification

Usage: aureum run [OPTIONS] <PATHS>...

Arguments:
  <PATHS>...  Paths to config files

Options:
      --format <FORMAT>            Options: passthrough, toml [default: passthrough]
      --default-timeout <SECONDS>  Fallback timeout for programs without a timeout; only used with `--format toml`
      --scratch <MODE>             Options: per-test, in-place [default: per-test]
      --scratch-root <PATH>        Root directory for per-test scratch directories. If omitted, a system temporary directory is used
      --keep-scratch               Do not delete per-test scratch directories after the run
      --verbose                    Print extra information about config files
  -h, --help                       Print help
"""
expected_stderr = ""
expected_exit_code = 0
