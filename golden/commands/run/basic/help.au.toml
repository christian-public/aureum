program = { from_env = "AUREUM_TEST_EXEC" }
program_arguments = ["run", "--help"]

expected_stdout = """
Run programs and print their output

Usage: aureum run [OPTIONS] <PATHS>...

Arguments:
  <PATHS>...  Paths to config files

Options:
      --format <FORMAT>            Output format [default: passthrough] [possible values: passthrough, toml]
      --default-timeout <SECONDS>  Fallback timeout for programs without a timeout; only used with `--format toml`
      --scratch <MODE>             Where each test runs [default: per-test] [possible values: per-test, in-place]
      --scratch-root <PATH>        Root for per-test scratch directories [default: system temporary directory]
      --keep-scratch               Preserve scratch directories after the run
      --verbose                    Print extra information about config files
  -h, --help                       Print help
"""
expected_stderr = ""
expected_exit_code = 0
