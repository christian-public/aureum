program = { env = "AUREUM_TEST_EXEC" }
program_arguments = ["list", "--tree", "helpers/*.toml"]

expected_stdout = """
/
└── helpers/
    ├── with_subtests.toml
    │   ├── :test1
    │   └── :test2
    └── without_subtests.toml
"""
expected_stderr = ""
expected_exit_code = 0
