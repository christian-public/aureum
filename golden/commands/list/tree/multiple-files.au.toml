input_files = ["helpers"]
program = { env = "AUREUM_TEST_EXEC" }
program_arguments = ["list", "--tree", "helpers/*.toml"]

expected_stdout = """
/
└── helpers/
    ├── with-subtests.toml
    │   ├── :test1
    │   └── :test2
    └── without-subtests.toml
"""
expected_stderr = ""
expected_exit_code = 0
