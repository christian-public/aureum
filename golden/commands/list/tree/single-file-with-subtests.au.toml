program = { env = "AUREUM_TEST_EXEC" }
program_arguments = ["list", "--tree", "helpers/with-subtests.toml"]

expected_stdout = """
/
└── helpers/
    └── with-subtests.toml
        ├── :test1
        └── :test2
"""
expected_stderr = ""
expected_exit_code = 0
