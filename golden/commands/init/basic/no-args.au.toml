program = { from_env = "AUREUM_TEST_EXEC" }
program_arguments = ["init"]

expected_stdout = ""
expected_stderr = """
Initialize a new config file

Usage: aureum init [OPTIONS] [PATH] [-- <COMMAND>...]

Arguments:
  [PATH]        Where to write the config file (recommended extension: .au.toml)
  [COMMAND]...  Program and arguments to record output from

Options:
      --print  Print to stdout instead of writing to a file
  -h, --help   Print help
"""
expected_exit_code = 2
