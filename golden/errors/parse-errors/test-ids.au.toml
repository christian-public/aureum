program = { from_env = "AUREUM_TEST_EXEC" }


[[test]]
id = "missing-id"
program_arguments = ["validate", { path_of_file = "helpers/missing-id.toml" }]

expected_stdout = """
✘ helpers/missing-id.toml  Parse error
"""
expected_stderr = """
📋 helpers/missing-id.toml
└── Parse errors
    └── ✘ line 4: [[test]] #1: missing required field `id`

warning: some config files contain errors (see above)
"""
expected_exit_code = 3


[[test]]
id = "invalid-id"
program_arguments = ["validate", { path_of_file = "helpers/invalid-id.toml" }]

expected_stdout = """
✘ helpers/invalid-id.toml  Parse error
"""
expected_stderr = """
📋 helpers/invalid-id.toml
└── Parse errors
    ├── ✘ line 12: [[test]] #2: field `id`: invalid identifier `invalid id!` (allowed: ASCII letters, digits, `_`, `-`; separate nested ids with `.`)
    └── ✘ line 19: [[test]] #3: field `id`: invalid identifier `` (must be non-empty)

warning: some config files contain errors (see above)
"""
expected_exit_code = 3


[[test]]
id = "id-at-root"
program_arguments = ["validate", { path_of_file = "helpers/id-at-root.toml" }]

expected_stdout = """
✘ helpers/id-at-root.toml  Parse error
"""
expected_stderr = """
📋 helpers/id-at-root.toml
└── Parse errors
    └── ✘ line 5: field `id` is not allowed at the root level

warning: some config files contain errors (see above)
"""
expected_exit_code = 3


[[test]]
id = "duplicate-id"
program_arguments = ["validate", { path_of_file = "helpers/duplicate-id.toml" }]

expected_stdout = """
✘ helpers/duplicate-id.toml  Parse error
"""
expected_stderr = """
📋 helpers/duplicate-id.toml
└── Parse errors
    ├── ✘ line 19: [[test]] #3: field `id`: duplicate identifier `shared`, first defined on line 5
    └── ✘ line 26: [[test]] #4: field `id`: duplicate identifier `shared`, first defined on line 5

warning: some config files contain errors (see above)
"""
expected_exit_code = 3
